// Code generated by entc, DO NOT EDIT.

package ent

import (
	"entgo.io/ent"
	"entgo.io/ent/dialect"
)

// Option function to configure the client.
type Option func(*config)

// Config is the configuration for the client and its builder.
type config struct {
	// driver used for executing database requests.
	driver dialect.Driver
	// debug enable a debug logging.
	debug bool
	// log used for logging on debug mode.
	log func(...interface{})
	// hooks to execute on mutations.
	hooks *hooks
}

// hooks per client, for fast access.
type hooks struct {
	Activity              []ent.Hook
	ActivityComponent     []ent.Hook
	AdChangeLog           []ent.Hook
	AdChannel             []ent.Hook
	AgreementLog          []ent.Hook
	AmBalance             []ent.Hook
	AppVersion            []ent.Hook
	AssetChangeLog        []ent.Hook
	AssetItem             []ent.Hook
	BookPackage           []ent.Hook
	ClientError           []ent.Hook
	CustomerNovelConfig   []ent.Hook
	CustomerNovels        []ent.Hook
	DataSource            []ent.Hook
	ListenRecord          []ent.Hook
	Novel                 []ent.Hook
	NovelAutoBuy          []ent.Hook
	NovelBookshelf        []ent.Hook
	NovelBuyChapterRecord []ent.Hook
	NovelBuyRecord        []ent.Hook
	NovelChapter          []ent.Hook
	NovelClassify         []ent.Hook
	NovelComment          []ent.Hook
	NovelConsume          []ent.Hook
	NovelMsg              []ent.Hook
	PayOrder              []ent.Hook
	SocialUser            []ent.Hook
	TaskLog               []ent.Hook
	UserEvent             []ent.Hook
	UserMsg               []ent.Hook
	VipUser               []ent.Hook
}

// Options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...interface{})) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}
